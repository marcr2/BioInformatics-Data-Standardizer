# BIDS - Bioinformatics Data Standardizer
# Requirements

# Core
python-magic-bin>=0.4.14
pandas>=2.0.0
numpy>=1.24.0

# Archive handling
py7zr>=0.20.0
rarfile>=4.1
openpyxl>=3.1.0
pyarrow>=14.0.0

# ML/Vectorization
scikit-learn>=1.3.0
chromadb>=0.4.0

# Local LLM (100% local, no API calls)
# NOTE: PyTorch is installed separately in install.bat with CUDA support
# If installing manually, use: pip install torch --index-url https://download.pytorch.org/whl/cu128
# (CUDA 12.8 supports Blackwell/RTX 50-series GPUs with compute capability 12.0)
# For CPU-only: pip install torch
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# GUI
dearpygui>=1.10.0

# Utilities
python-dotenv>=1.0.0
